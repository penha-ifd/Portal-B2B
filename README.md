
  # Tela principal do CRM

  This is a code bundle for Tela principal do CRM. The original project is available at https://www.figma.com/design/q2nRbe4aisHrRmCirgNsgG/Tela-principal-do-CRM.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  